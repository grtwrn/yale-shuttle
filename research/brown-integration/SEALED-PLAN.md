# Real K5/K8 contract follow-up

Bounded hosted synthetic integration only, pinned before implementation. The
original [results](RESULTS.md) remain historical and unchanged. No captured
September 23–29 response, physical outcome, fit tuning, training change or
production change is authorized by this test.

Load the actual September 23 K5 artifact from run 35696391115, code
`76cab36799d6fd43a8ae33c235eb299b0028040b`, ID
`97ff8586a3760e4a3696689510008f9a972b94b989cf793b52bcd4ad457fd54f`.
Its builtAt is `1790059735660`, trainBefore `1790049600000`, validFrom
`1790136000000`, and exclusive validUntil `1790222400000`. Retain these bytes
exactly. The artifact's unchanged original-fit runtime SHA256 is
`9e5ca3bb06140305a005c14f3ccfd634e866f6808c1b356f4b0bf7d640697785`.
Use its supported loader for both K5 and the already sealed K8 artifact
`d8648c2a87c00a113268e5da49ef6a8fa46c62081f740ac9bf8152fb7f648c76`.
Verify every sealed source/pool/input hash and keep manifests kind=sealed;
never move the real build or validity timestamps to the old development dates.

The three previously fixed synthetic development cases will be translated by
whole days to September 23, preserving their time of day, relative clocks,
phase, target occurrences and original complete wire cells. This is an
explicit synthetic clock fixture, not a claimed future raw trace or capture.
Prefix digests bind the original fixture and transformation. Collect all target
queries before support, execute them with the real sealed Python pools, then
feed the complete overlays to the same pinned frontend checkout `4f5d261`.
No candidate labels or original development forecast-equality claim apply to
the newly queried real K5 pools.

Required gates:

- Both exact manifests and runtime hashes load; unchanged fit AST is used.
- Real build time and validity boundary tests: before build, at build, before
  validity, at validity, last valid millisecond and exact expiry. Server clock
  controls availability even if receipt time has crossed a boundary. No K5
  reuse on September 24. Synthetic late-build fixtures separately isolate the
  build boundary without changing the real artifact's manifest.
- Whole-target query accounting and missing-result errors remain distinct from
  explicit null unsupported fits. Wrong model/pool/source/request bindings fail
  closed. Invalid wire, missing clock and unsupported full groups keep the exact
  served body; no fixture-artifact override is enabled for the real model tests.
- Every non-low/high cell and original distribution stays unchanged. Both
  frontend profiles and all four independently mounted arms match the original
  component; reversed execution order remains identical. Actual array attachment
  and public ETA expiry retain their existing semantics.
- Rerun the existing response/selection/development controls unchanged; append
  new sealed-contract evidence separately. Preserve both production trees.

The K5 provider proof concerns source departure through target arrival. The
single historical target closes after a later provider reissue, while its
arrival anchor and all 504 quality-bracket fixes match the source provider.
This follow-up neither refits nor changes that rule. It does not claim provider
continuity through a later boarding departure, which remains a separate gate.

The old distribution dots still accompany changed bounds. Passing this test
establishes sealed-model integration on synthetic inputs only, not fresh support,
outcome accuracy, coherent probabilities, complete-capture readiness or launch.
