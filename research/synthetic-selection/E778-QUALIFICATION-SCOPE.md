# Deployed e778 source qualification — frozen execution scope

Pinned before tests, 2026-09-22. Research branch descends from 53834e9;
the completed 8c67b5b benchmark, 28,224-profile evidence and original source
proof remain unchanged. No prospective bodies, physical outcomes, candidate
models, production edits, new recording, or full benchmark rerun are permitted.

## Source and identity

Qualify only the exact frontend trees for:

- 05a988194af3c376e5aa5da16682c29f797db2b2:
  39e7e9738975f45dfb5c443cc99961a39e9aa4ef (existing baseline/default).
- e7784c03b8c739eaa603fb231fe5ce80d2d71e82:
  f985c086ee1d340ab37c7fc7c482b98bcde03ac6 (complete web tree;
  c86bcf558a276752ed35b1145bd7c7fc33f7d4cf is its web/src subtree).

The first hosted proof attempt rejected the previously supplied subtree hash
as a complete-tree identity. This correction follows exact git tree identity;
no source, behavioral threshold or scenario changes accompany it.

An explicit source argument selects an allowlisted qualification. Extraction
checks the entire checked-out frontend against that source and records the
loaded identity in the generated module. Runtime release binding must match
that loaded identity, never merely another registry entry. Unknown sources,
changed source, changed/missing/extra assets, and mismatched loaded identities
fail closed. The intermediate b008 source is outside this qualification.

Each hosted job stages one exact source in an ephemeral checkout; no process
shares frontend/React globals between versions. Default remains 05a. Future
multi-version dispatch is separate work. Original proof bytes/knownAt are
immutable; e778 gets its own served-versus-built asset proof and actual proof
availability time, with no retroactive release assignment. Continuity remains
policy 9ded9d0: exact initial receipt, unknown shadows old, no favorable-start
search, and a bound synthetic page retains its source for its fixed horizon.

## Exact change and relevance

Inspect and record the complete 05a-to-e778 diff. The frontend changes are
planner's overview walking threshold (two thirds to one half), hackerspace
icon, and source tests. Backend adds MakeHaven/aliases; fixed coordinate
scenarios do not call geocoding. Verify these additions against source without
introducing destination search or changing any synthetic geometry.

## Hosted gates (bounded, synthetic only)

Run both versions' existing component, exact parent-poll, envelope and streaming
fixtures, retaining original-component versus extracted-adapter comparisons.
Add fixed meaningful fixtures for exactly half walking, one second above,
between half and two thirds, exactly two thirds, and one second above that;
keep full options unchanged and prove the actual component visibility path
uses its version's exports. Include current-source overview regression tests,
unknown/cross-version rejection, future proof rejection, and bound-page
continuity across a known other version. No outcome-driven fixture search.

Rebuild exact deployed e778 assets on a hosted runner and compare byte hashes
to saved live verification evidence. Record extraction hashes and per-version
test counts. Existing 05a evidence is not regenerated or relabeled. The gate
workflow is bounded to two version jobs and 20 minutes each; no scale workflow
or capture runner is invoked. Results qualify component state and source
identity, not DOM/browser behavior, physical boarding, ETA accuracy, or every
possible release between capture health brackets.
