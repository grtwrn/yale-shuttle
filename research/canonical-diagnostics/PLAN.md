# Frozen-cohort exclusion and support diagnosis

This diagnosis reuses canonical run 35684356219 and its original k-sweep input run 35677536788. It changes no threshold, estimator, forecast, wait classification, training cutoff, source, or label cohort. All substantial work runs on GitHub hosted runners.

For each frozen/daily cutoff, independently enumerate the exact existing training paths and assert equality with the canonical model, including Red's 45-minute cap. Count source and target validity, traversal/cap failures, and each GPS quality condition and their combinations. Quality reasons reproduce the existing bracketing logic exactly, including its 10-second boundary tolerance, 60-second maximum edge, 22 m/s speed ceiling, route continuity and provider identity requirement. Duplicate timestamps are divided into identical versus conflicting fixes; simultaneous distinct provider IDs are described as observed contention, without inferring which identity is correct. These annotations cannot alter admission.

Reattach every outcome to the prior unscored rows and compare exact labels with the frozen scored cohort. Recompute every candidate and its evidence and compare with the frozen unscored artifact. For every observed fitting context, inspect every target in its complete group, including targets beyond the first failed target. Report raw path/source/day counts, weekday-matched counts, clock-weighted effective count, material date counts and exact fit failure conditions. Report source availability and pre-fit prediction gates separately; absent waits and absent observed fitting contexts are availability findings.

Publish only public fleet bus IDs, times, route/occurrence indices, derived spacing/distance/speed and aggregate evidence. High-speed edge records include adjacent edge spacing/speeds and out-and-back distance; they are observations, not a corruption classification. No coordinates, reports or rider information are included. No threshold relaxation or model selection follows automatically from these results.
