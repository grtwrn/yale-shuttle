# Frozen Brown K8 export sealed before prospective use

[Hosted run35694227486](https://github.com/grtwrn/yale-shuttle/actions/runs/35694227486)
passed at `434afd324fb39d8f4ee2f0dfe7df6d58f9805f92`. Artifact:
`sealed-frozen-brown-k8`, retained for90days and downloaded locally as
`sealed-brown-k8-35694227486/`.

- Artifact ID: `d8648c2a87c00a113268e5da49ef6a8fa46c62081f740ac9bf8152fb7f648c76`.
- Actual builtAt: September22,2026,06:19:00.242UTC (`1790057940242`).
- Training knowledge cutoff: September16,00:00ET, strict.
- Fixed validity: September23,00:00ET through September30,00:30ET, exclusive.

All264 Brown K8 paths across nine source/wait/target cells matched both saved
original and directed physical-path references. All167 saved frozen fit queries
matched both references exactly, including49 unsupported queries. Removing all
future raw/visit suffixes preserved the paths and fits. Reloading the sealed
pools and executing the exact original fit/helper ASTs also matched every query.
The exported causal prefixes contain805,357 raw rows and25,975 completed visits;
these are all-route quality/training provenance, not independent Brown samples.

Six hosted fixture groups passed: exact-source/fractional serialization,
unsupported support, weekday/weekend separation, invalid/foreign pools,
fingerprinting changed helper code, and actual-build/validity boundaries.
Production source was not changed. No prospective observations, performance
labels, new parameters, winner selection or live activation were involved.

An initial run35694181130 failed the protocol-hash gate before fitting: the prose
companion hash had been supplied where the machine JSON hash was required.
The final manifest explicitly uses JSON lock hash
`4d1e09bd3cb41762a11e19a7de49bcfb8d8c479b44b3354a845df1b6c8f82742`.

Use `runtime.load(directory, expected_artifact_id)` to verify sealed files and
load the original fit over immutable pools. A returned unsupported fit is
different from a missing query sidecar or missing model; downstream adapters
must preserve that distinction. Forecasts before actual builtAt, outside fixed
validity, or without a verified artifact remain exact served fallback. This
new export cannot claim availability during September17–20 development replay;
those adapter fixtures must remain explicitly synthetic.

This closes only the frozen-model availability step. Rolling K5 exports,
complete raw/public alignment, complete-response and selection integration,
independent physical outcomes and fresh support remain separate gates. The
protected hybrid still does not supply a coherent new50-point distribution.
