# Availability and displayed-width diagnostic

Reporting only over protected run 35690386363 (edc84d427b6401db0a440360ef9a7940b01c3b90), its unchanged labels, unscored features and saved action comparisons. Use the pinned canonical topology/wait map from run 35684356219 solely to identify the requested source occurrence and unsupported target. No fitting, replay of GPS, new outcomes, new dates, threshold changes or candidate selection.

Keep both fixed highway policies and all 14 arms. Count each recorded raw fallback/support/release reason on all generated snapshots and on existing labelled snapshots/physical visits. Reasons overlap across a physical visit, so visit-level reason counts are nonexclusive. Decompose only causes directly observable in saved features: current observation age, phase/index, requested source present, and stored release latch/progress conditions. Missing origins were already filtered by the 45-minute cap; warm first/last state was not retained. Never attribute missing sources specifically to age, resets, absent emissions or warmup when those cases are indistinguishable.

Separate raw forecast equal/narrower/wider than deployed, actual protected forecast equal/narrower/wider, protection eliminating a change, and raw/protected changed physical visits. Existing label IDs supply denominators only. Compute actual displayed width through the production predictionWindow helper at the forecast instant, deriving outward minute endpoints from its exact printed text; '<1' denotes a zero lower endpoint and absent windows are counted separately. Report visit-weighted printed widths on full routes, fixed raw all-arm union and protected all-arm union, retaining every arm. Preserve action-censoring and paired extra-wait summaries from the finished study; do not rerun action rules.

These diagnostics explain observed availability and presentation, not calibration on unseen service or full application route-selection safety. They should motivate a falsifiable future measurement, not a threshold relaxation or the smallest observed window.
