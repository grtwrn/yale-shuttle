# Chronological ensemble residuals: execution plan

Development-only follow-up to the residual stage pinned in `../checkpoint-ensemble/PLAN.md` at `bce95f6`. Base implementation and completed joint/action study: `adfe3bcfd557cbd93ba2bf58a7a46ddb3a27b1d5`. The negative joint/action results do not select or tune this estimator. This document fixes execution details before new fitting, residual construction or scores. Parent review precedes hosted execution.

## Immutable scope

Use only the previously pinned raw/prediction bytes, canonical topology/training visits, source artifact from run 35740424751, and exact completed evaluation streams from run 35741682149. Source SHA256s and artifact identities remain those in the ensemble plan and verification files. Evaluation is the same September 17–20 sampled keys and immutable highway25/highway50 labels; original22 versus highway additions remain separate. No September 21+ data, production edits, resource/schedule changes, new K, cap, threshold or weight search. All computation and synthetic tests run on GitHub-hosted runners. Pi work is limited to source editing, git and compact metadata/results.

Keep K5/K10, frozen/rolling, primary live handoff/separate post-wait extension, ensemble/matched single, all 14 routes and every unavailable cell. Original points, deployed triples, old single-K arms and evaluation labels remain immutable controls. Residual intervals are the only new estimator. The existing rejected joint intervals remain documented controls; they do not gate or train residuals.

## Stage A: causal folds and a necessary-support ledger

For every actual historical forecast date D preceding the latest bank cutoff (September 19 00:00 ET), calculate its fit/map cutoff D−1 00:00 ET. Classify waits from canonical completed visits with arrival <= departure <= actual knownAt < cutoff, non-gap passed/stopped outcomes and finite stand time, using exactly n>=30, at least three arrival dates and linear-interpolated stand p75>=180 seconds. Preserve every route/occurrence's counts, dates and decision. The September 16 classification must reproduce the pinned evaluation wait map and statistics. No raw or highway-quality rule is added to wait classification.

Rematerialize causal membership with the fold map. Reuse only wait-independent evidence, not earlier rows' September 16 wait selections. A universal observer seeds source families for each canonical stop occurrence at actual strict emission. It applies the existing identity epochs, missing-offset invalidation, unwrapped phase progression, irreversible 45-minute expiry, one-way release and first-fixed-group-pickup phase retirement continuously. These operations do not depend on which stops qualify as major waits. A query projects this retained causal evidence onto that date's qualified wait and exact downstream group. This is no insertion of finalized visits and no resurrection when a map changes. A valid source is at most 45 minutes old, so its emission and physical source are later than that fold's map cutoff, which is at least 24 hours before the forecast date begins. Assert these clocks rather than relying on the description.

The universal observer's projection onto the original September 16 map must reproduce all original 133,435 compact feature/membership rows, strict source IDs and occurrence proofs from run 35740424751. Also fixture-check multiple qualified-wait maps against separate original ledgers: the first downstream occurrence remains wait+1 for every nonempty group, and target selection still proves the same upcoming pickup. Preserve the original replay lead-in boundary at September 15 23:00 ET and label it as a research boundary. Existing 15-second freshness, ten-minute experimental continuity warmup, observed-provider/name resets, target occurrence/hop agreement, exact contiguous masks and whole-group rules are unchanged. Provider IDs establish observed continuity, not proven physical identity. No EOF closure. Stream compact forecast references and hashes; retain immutable source records once, and do not serialize universal family snapshots per forecast. Prefix runs execute sequentially and compare streaming hashes/counts, without retaining several complete runs in memory. These are representation changes only, not evidence/sample cuts.

Stage A reports all original sampled keys, fresh/warm keys, fold-qualified groups, membership reasons, exact masks/regimes, source IDs/knownAt, farthest-source departure date/day type, forecast date, and candidate journey counts. A fold row can contribute only to an evaluation stratum with the same wait occurrence and exact whole target-group tuple. Retain mismatches and midnight crossings explicitly. This stage does not admit calibration target outcomes or compute prediction errors; historical visit arrival fields remain necessary for causal wait classification.

For each frozen/rolling bank cutoff and evaluation query stratum, count distinct possible source journeys and possible farthest-source dates before the cutoff, matching day type. These are necessary ceilings before model support, countdown latches, target outcome admission, physical-target deduplication and weighting. Fewer than 12 possible journeys or three possible dates proves that cell cannot pass; otherwise label it **not ruled out**, never supported. No prediction rows, repeated masks, estimator copies or query snapshots count as extra journeys. Missing GPS on an ET calendar day alone is not a source-date impossibility proof: enumerate actual sources, including adjacent-midnight forecasts. Do not infer effective/material-day support from raw ceilings. Report hypothetical cells for every route/K/mask/regime even when no evaluation query reaches them, separately from query-exposed cells.

Upload a small route/stratum ceiling report and full ledger. The first authorized execution is **Stage A only**, followed by parent review. If every exposed cell is rigorously impossible, complete and report the causal support result plus residual-mechanism fixtures; do not spend fitting/scoring work to manufacture an interval. Retain the remaining implementation stage as explicitly unexecuted. If any exposed cell is not ruled out, the following stages specify the already-pinned estimator; the ceiling cannot select a favorable estimator or relax a gate.

## Stage B: chronological points before outcomes

Build each date D's constituent model from its own D−1 prefix, with that fold's waits and the unchanged strict path/quality/occurrence proof. Keep both highway policies independent. Rebuild proof/identity evidence from prefix-only inputs in the first/last-cutoff parity checks. Use the same marginal time-of-day weighted duration means, support >=12 effective paths / three >=5%-weight source dates, weekday/weekend split, path caps and all active offsets. **Do not require complete joint-vector support.** Extract a marginal-only group helper and prove its supported points/component support/countdown outcomes exactly match the existing helper with joint calculations ignored.

Process the original sampled forecasts in time order. Maintain one irreversible countdown-expiry ledger across forecast dates/model refresh, keyed by K and the physical source journey (including wait occurrence), shared across masks, primary/extension and ensemble/single. A change in map/group does not erase an existing expiry. Families and raw source evidence already run continuously; each fold model may only query its qualified map. Report map/group transitions. Do not create synthetic earlier snapshots to prime the latch. Before every asof, all fit evidence and map decisions are already known. Whole-group model or countdown failure is unavailable, not partial averaging.

Freeze chronological unscored point records, all constituent absolute arrivals, exact source/mask/group identities, support, latest training emission and rejection reasons before opening calibration outcomes. A source traversal used for a forecast lies after its fit cutoff; assert its target and sources cannot enter that fit. No same-journey training/calibration reuse.

## Stage C: actual-known outcomes and fixed residual banks

At each frozen bank cutoff (September 16 00:00 ET) or rolling evaluation bank cutoff (previous date 00:00 ET), admit only target outcomes physically arrived and actually emitted before cutoff, with the full required source/forecast-to-target evidence and all quality bracketing fixes before cutoff. Use the first compatible physical pickup; an earlier unresolved or incompatible same-stop opportunity makes the outcome unavailable. Do not skip it to a later occurrence. Preserve skipped-before-wait training visits separately from still-upcoming pickup opportunities. Build calibration labels separately; never replace the immutable September 17–20 evaluation labels.

Keep exact fixed strata: quality, route, wait occurrence, whole target-group tuple, target occurrence, K, full active mask, pre/post-wait regime, farthest-source weekday/weekend, and ensemble versus matched single. Primary and extension share the same pre-wait residual evidence; extension's post-wait mask is separate. Deduplicate the first chronological eligible sampled forecast for each physical target/source traversal/mask/regime/forecast date. Across retained date repeats within a stratum, allocate one physical target's base mass equally among those repeats, total <=1; then combine repeated rows of the same physical source traversal for effective-support calculations. Report repeats and distinct physical journeys/targets/dates explicitly. Different strata are separate estimators, not independent samples or additional support for one another.

Weight base mass by the unchanged 120-minute circular Gaussian comparing farthest K-source departure clocks and matching its day type. Count material dates by that source departure date. Require >=12 effective distinct journey weights and at least three dates each carrying >=5% of total weight for every target in the fixed group. Missing sampled outcomes at any target remain missing; do not substitute training paths as residuals or pool masks/groups. Exclude any evaluation source journey/physical target from its own bank even if malformed chronology otherwise allowed it; assert no such overlaps occur under the cutoff.

Residual is actual absolute target arrival minus that fold's unchanged absolute point. Weighted empirical q10/q90 produce low=min(point,point+q10), high=max(point,point+q90). Subtract query clock and clamp/round once with the existing wire helper. No point/bias adjustment, weights selected by error, averaged bounds, covariance-based shrinkage or sqrt(N). The fixed residual plan does not add a protected-lower variant. Unsupported group calibration returns the exact deployed triple. Report any later lower bound and all action consequences; the empirical construction carries no guaranteed coverage claim.

## Stage D: immutable evaluation and required controls

Freeze bank provenance, support decisions and all unscored interval streams before joining the exact immutable development labels. Reuse all-route/full-denominator, common supported-union, original/addition, raw/actual printed window, visit-normalized error/coverage/early-tail, ordered-target, handoff and fixed-physical-visit action reporting from the completed study. Compare ensemble to deployed, immutable original single-K and matched single under the same residual mechanism. Preserve all walk/response settings, censors, missing arming history and zero-change cells; repeated settings are not independent riders. Earlier-fold models have shorter histories than final query models: report that transfer limitation. No fresh validation or full-app ranking safety claim.

Before interpretation require immutable source/label hashes, all original membership/point controls, source-reference parity, full calendar-day embargo, no EOF closure, and physically deleted-future parity for existing quarter/mid/three-quarter asofs plus first/last map/fit/bank cutoffs. Prefix tests rebuild folds and input-derived sets from their own prefix, not a global future-name/map cache. Synthetic hosted fixtures cover map qualification boundaries, future emission exclusion, midnight source dates, exact group/mask mismatch, continuous expiry across map/model refresh, no joint support dependency, first-pickup censoring, actual-knownAt admission, physical target/journey weight caps, shared errors, quantiles/translation and exact fallback. Full streams stay hosted; compact reports, counts and artifact hashes are the handoff.
