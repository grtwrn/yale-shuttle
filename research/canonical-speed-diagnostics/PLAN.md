# Predeclared speed and observation-time diagnosis

Use the unchanged frozen raw positions from k-sweep run35677536788 (SHA2563990d06ebdab596cfebdd7f03c528f7efcbb46fd3f6af68a9d64ede648e220b9). This is observational research only: no ETA fits, exclusions, thresholds, canonical forecasts or production settings change. All scans, fixtures and geodesic calculations run on GitHub hosted runners.

For every route, compare the existing latitude-scaled planar distance with GeographicLib2.1 WGS84 ellipsoidal geodesics on every stable-route/provider, positive-time edge over22m/s and matched low-speed controls. Match within the same public bus/provider/route/calendar date and rounded one-second cadence bin, choosing closest collection time (earlier on a tie), separately allowing stationary controls and requiring moving controls. Expose matching distance in time; unmatched cases stay unmatched. Compare other urban lines using this same algorithm. No model-error or coverage information is used for sampling.

For Green and Purple, select exactly three diagnostic edges each: earliest by time, median by planar speed, and maximum by planar speed, with bus/time tie breaks. Preserve a ±30-second track around each plus nearest boundary observations. Also report those same selections for other lines as comparison examples. Report stable high-speed edge concentration by bus, provider, date and route, with all stable edges/moving edges as denominators.

Describe overlapping patterns without adjudicating truth: adjacent high edges, isolated threshold crossing, identical-position polls before/after, short collection intervals, immediate return toward the prior position, and thirty-second surrounding net/path speeds. A speed computed from the first poll of an identical-position run is a timing proxy, not a recovered sensor timestamp. Neither smoothing nor the proxy changes any gate. Any candidate interpretation must retain this limitation.

Audit archived schema and collector source. The archived collected_at is the local fetch-completion timestamp, shared across a poll and made monotonically increasing; upstream lastUpdate is parsed but not persisted. Raw lat/lon are copied from the upstream bus response before detector/map matching. No archived device measurement time, request-start/response latency or full upstream envelope permits proving actual observation cadence or upstream interpolation. No additional upstream sampling will be performed.
