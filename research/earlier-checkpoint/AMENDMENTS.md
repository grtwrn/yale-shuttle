# Audit trail

The initial plan and numeric model families were fixed before scoring. No application model was changed.

- Initial hosted run, commit `18e33cd`: frozen historical rows, eight training dates, September 16 calibration, September 17/18/21 evaluation. Both future-data and prefix-replay checks passed.
- Commit `505ef89`, made before reading initial scores: use the actual detector anchor for “five stops behind,” rather than the current hold/drive phase index; discard origins belonging to a completed previous target occurrence; handle missing logged bounds consistently. These are implementation/identity corrections. All primary scores are rerun; the initial run remains available.
- Commit `4f5ecc5`: extend the same-day capture without refitting training history or calibration. The capture has additional bus GPS/visits but **no additional rider forecast log rows**. It therefore does not provide new prospective comparisons against production. Add raw-case audits, common-origin jump comparisons, and the prespecified input-gap sensitivities. A before-17:00 sensitivity was added after seeing a closing-time regression and is explicitly exploratory; it cannot select or validate a promoted model.
- Training-path quality sensitivity: the initial transfer included raw positions only on evaluation dates. Training used strict connected stop/leg chains, omitting training paths whose fragmented labels could have been repaired with raw GPS. Add **all** locally available pre-September-16 Red raw positions, using the already-declared continuity rule across all eligible sources and targets. No individual outcome is selected or deleted based on forecast error. Repeat the same numeric models, support limits and calibration rule. Earlier outputs remain separate so the effect of this input-quality expansion is measurable. These are reused evaluation days, not a newly untouched holdout.

The later capture was taken after the model freeze at 17:47:50Z. Forecast origins before that clock are a newly inspected retrospective extension; only later origins could qualify for a prospective extension. A prospective result is not claimed without both outcomes and a comparator.

The final freshness sensitivity uses the same algorithms with completed history through midnight before each evaluation day, retaining the September16 interval padding. It tests whether the frozen older history disadvantaged the candidates relative to continually refreshed production. It was added after initial scoring, does not tune or select algorithms, and has a per-day future-outcome deletion invariant. Frozen-history results remain the primary comparison.

The user then requested ten-stop and wait-relative anchors, clarifying that K counts backward from 344 Winchester, not forward. `FOLLOWUP-PLAN.md` specifies six variants before their scoring. Expanded causal checkpoint features are kept separate from the original origin map, and the original outcome cohort stays fixed. The K=2 departure model must exactly reproduce the prior five-before-pickup model. This follow-up explicitly assesses the user's preference for narrower windows at a modest point-error cost; it adds nominal80/90% calibration and a production interval-contraction control using September16 only. It is exploratory research on the same dates, not new validation or deployment.
