# Independent review 14 — cycle-15 full-fleet slice

**Verdict: research_only. One remaining outcome-label defect blocks treating the final arrival scores as validated evidence. No application proposal or production approval.**

Exact supplied base, supplied head, independently read HEAD and merge-base are `98e535b99649e74ca599d2e33bcfdc46df83d30d`; tree `d0ab5a7678483e956bd1e4072abe92b18a464218`. Candidate, working-tree and index diffs are empty. Reviewer outputs are in `review-round-14/`. The underlying cache diagnosis remains credible; neither a coefficient change nor release is authorized.

## Required correction: latest detector source is not always the priced current visit

**[P2] `cycle-15/score.py:104–110` still shifts a lagged current stop and its following occurrence into later laps.** The new zero-hop rule only applies when `source['stop_index'] == targetIndex`. Historical anchoring can already have advanced to the next stop while the estimator retains the previous rest. Then `sameSource` is false, modulo distance treats the old current stop as a future return, and adding `occurrence * N` moves the following row one lap too far. Verifying a connected chain and its modulo distance does not establish that it belongs to the forecasted visit; the submitted semantic checker repeats that source-alignment assumption.

A concrete case, independently read from the original database and captured wire:

- Poll 220, `1789557783285`, Orange Day #53, physical index 1 / stop 28.
- Both arms emit a zero-hop row `[eta, low, high, hops] = [0, 0, 0, 0]`, plus a following row at 33 hops. The tracked rest is index 1 with origin `1789557703290`.
- That origin exactly equals visit **56897**'s pin. It arrived at `1789557708279` (**75.006 seconds before** this poll) and departed at `1789557733440` (49.845 seconds before it).
- Reached leg **52575** connects that departure to the next anchored source **56906**, physical index 2. The scorer selects 56906 because its anchor already occurred.
- The scorer assigns the zero-hop row to **57352**, the next visit to stop 28, **3,127.679 seconds after** the poll. That changes the forecast's visit identity; it is not a legitimate 52-minute error for this already-arrived row.

The independent audit conservatively requires an exact old pin-clock match, a prior departure and an exact reached leg into the scorer's newer source. It proves **11 paired poll-target cases across 10 prior visits and five routes (1, 2, 3, 8, 15)**. Eight corresponding following rows are also scored one lap too far. For example at poll 1227, Orange #49's following row is assigned visit 58554 at +6,781.658 seconds instead of first return 58178 at +4,265.453 seconds. All original forecast rows and historical records remain untouched.

The complete scored cohort contains 68 current-arm / 69 diagnostic zero-hop rows. **Do not classify or discard all of those as errors.** The proof above establishes only 11; the others need their own identity evidence. Correct priced-origin/current-versus-following alignment, retaining already-arrived rows in explicit transition accounting, following arrivals with the right connected target, and genuinely ambiguous cases as unresolved. Audit nonzero rows affected by the same alignment question as well. Do not change the estimator, cap ETAs, alter historical records or drop valid bad forecasts to improve scores.

The required gate `python3 review-round-14/audit_lagged_origin.py` exits **1** on this specific final-label defect after writing all proof records. `audit_following.py` independently confirms the eight shifted following rows. These are research-artifact corrections, not an application patch request. Preserve cycle-15's final and superseded outputs before the builder updates the label contract.

## What independently reproduces

- Fresh builds from the exact current source equal both frozen replay bundles after normalizing only the two entry-file path comments. The entire arm difference is exactly one substitution: compute the movement kernel at the rounded mean already used by its cache key. The original first-caller cache remains in the current arm. No forecast input reads a future neighbor trajectory or future outcome; outcomes enter only scoring. Current release and sampled future laps are enabled in both arms.
- The current planner memo extracted in `shell-current.generated.mts` is an exact substring of current `TransitMap.tsx`; only surrounding instrumentation suppresses telemetry and records decisions. Fresh execution reproduces both decision streams byte-for-byte.
- The prior reviewer correction landed: capture serializes each wire immediately, clones poll-time model state before hashing, and checks that saved belief timestamps are not in the future. The cycle-14 shallow-reference evidence is explicitly superseded, with originals retained.
- I executed fresh-process continuations from each frozen poll-5900 native checkpoint. Each arm reproduces **100/100 complete poll records**, including eight immutable minute-state digests. This validates research chunking with the explicitly saved kernel cache. It is not a claim that the production checkpoint captures that cache, nor another production restart fix.
- Complete rescoring of the saved 6,000-poll streams reproduces all **182,918** saved forecast/label records exactly, every aggregate score field, availability, 5,021 large band changes, and unknown-case accounting. Only equally ranked regression ordering varies with set iteration; cutoffs and maximum +69-second point-error regression agree. Database verifiers traverse 86,683 distinct chains / 1,920,044 leg uses. Those paths exist, but the new defect above shows some are attached to the wrong forecast visit.
- The earlier corrections are preserved and reproducible: **125 retargeted records**, 187 newly scored, 182,606 unchanged, 1,323 moved to unresolved/past accounting. The cumulative-hop guard changes no additional labels in this slice. NULL endpoint handling does not invent a departure. The finding above is an additional source-alignment gap, not evidence that those 125 corrections failed to land.
- All **2,036** paired decisions across 20 declared sessions reproduce: no boarding-vehicle, visible-order, nested `journeyArrival.catchRisk`, destination-availability or synthetic 15/30/45-minute point-deadline differences. Fourteen differences concern relative-hop/visit metadata, not vehicle switches. The initial absent top-level caution-field mistake is corrected and its old script preserved.
- Both rider outcome files reproduce byte-for-byte: **4,925** same connected trips and **3,277** unresolved option-polls per arm; 48,072 ride-leg uses per arm independently verify. All 538 zero-hop connected rider pickups per arm retain `sourceId == boardVisitId`, so the demonstrated future-pickup relabeling is absent from that selected subset. This does not validate every possible pickup identity.

## Statistical and rider interpretation

The chronological runtime-bounded slice includes eight daytime routes, 6,000 of 20,199 polls, on September 16. Night routes and the later date remain unfinished. Calibration inputs are shared and pre-period; no new fit or coefficient search occurs. These are reused evaluation dates and selected stationary Red-origin rider geometries, not a new holdout or observed rider sample. Forecast generation uses reconstructed collector clocks, not recovered original publication times.

The submitted arrival arithmetic reproduces MAE **269.462900 → 269.452884 seconds**, median **182.1185 → 182.2635**, p90 **626.3398 → 626.4191**, WIS **176.951639 → 176.949988**. Before the additional label correction, those are provisional numbers, not validated final accuracy. Their tiny mixed direction provides no useful demonstrated ETA gain in any event. Dependent rows across 3,045 source / 3,885 target visits are not independent trials. Early misses mean the bus came before the displayed lower bound; late misses mean it came after the upper bound. Neither establishes nominal coverage or a calibrated on-time probability.

The independently reproduced selected rider outcome error slightly worsens: **352.172350 → 352.216208 seconds**. Actual target timestamps are combined with modeled walking, not observed egress. The 4,494 modeled access-before-departure cases are not proof of boarding; passed endpoints and raw-current transitions remain caveats. No browser or live class-deadline behavior is claimed by this numerical harness.

All 5,021 raw large band changes, including the +5,791-second upper-bound change, remain. Availability differences (206 unpaired physical keys; net +20 rows) are not a demonstrated benefit. Raw negative lower bounds remain 16,825 / 16,826; only scoring uses the existing rider adapter's zero normalization. The 16 backward numerical-lead events have identical time/bus/endpoints in both arms and are not silently declared physically correct. The 9,261 minute source-identity uncertainties remain explicit. The new proved transition rows must be added to that accounting correctly, not hidden. Previously audited legitimate Red regressions, 38 pickup unknowns and the older 442.827-second regression are untouched; this earlier-day slice does not newly validate or resolve them.

Earlier neighbor/service-role results remain exploratory and score-dependent. The modest Union follower log-loss signal coexists with flat/slightly worse Brier and date-confounded identity groups; none authorizes production coefficients.

## Commands actually executed

From the assigned worktree unless the wrapper changes into `services/shuttle-v2`. Let `O=/home/gwarren/projects/yale-shuttle-watcher/overnight-2026-09-17/eta/review-round-14`.

1. `flock -w 900 /home/gwarren/projects/yale-shuttle-watcher/overnight-2026-09-17/heavy.lock bash $O/run.sh` — **exit 0**. Checked sequential script: two fresh 100-poll continuations; `compare_resume.py`; full saved-wire `score.py`; `verify_score.py`; current-source `tsx decisions.mts`; `verify_decisions.py`; `rider_outcomes.py`; `verify_semantics.py`. Exact subcommands and separate logs are retained.
2. `flock -w 900 /home/gwarren/projects/yale-shuttle-watcher/overnight-2026-09-17/heavy.lock bash $O/check-build.sh` — **exit 0**. Fresh two-arm bundles from actual source; original-to-final join-change audit. Frozen bundles used for continuations preserved separately.
3. `python3 $O/independent_audit.py` — **exit 0**. Fresh/frozen/source provenance, actual memo-body equality, complete record reproduction, planner/rider bytes, original 125 retargeted rows and bounded zero-hop rider checks.
4. `python3 $O/audit_lagged_origin.py` — **exit 1, substantive label failure**. Full evidence in `lagged-origin-audit.json` and `lagged-origin.log`: 11 paired current-stop rows incorrectly assigned future targets. The earlier reviewer attempt incorrectly assumed every zero-hop row shares `tracking.restStop`; its source/log are preserved as `initial-rest-stop-assumption-*`. The final audit conservatively skips unproved rest identities and still fails on established cases.
5. `python3 $O/audit_following.py` — **exit 0**. Confirms eight following rows shifted one lap too far; this passing diagnostic is not a passing label gate.
6. `python3 $O/final_integrity.py` — **exit 0**. Exact head/base/tree, 905 tracked files, 140 builder files and eight frozen inputs unchanged; clean checkout/index. At capture: 400 shared images / 18,176,716 bytes; zero reviewer additions.
7. `git rev-parse HEAD 98e535b99649e74ca599d2e33bcfdc46df83d30d HEAD^{tree}`, `git merge-base HEAD 98e535b99649e74ca599d2e33bcfdc46df83d30d`, `git diff --exit-code 98e535b99649e74ca599d2e33bcfdc46df83d30d HEAD`, `git diff --exit-code`, `git diff --cached --exit-code`, `git diff --check`, `git status --short` — **exit 0**, exact values above, empty diffs/status.

Two guessed export filenames returned read-only exit 2; the existing `export-all-route.mts` was subsequently inspected. One later `rg` invocation returned 127; Python/grep fallback completed the inspection. Raw `diff` returned the expected 1 for the one-line arm difference and entry-comment differences; the independent equality assertions then passed. No application unit/type/full-suite/Vite/browser/Docker/staging/CI/deployment checks were run or claimed for this empty application diff. No dependency installation, screenshots, feedback access or external contacts.

## Resume and most useful next task

First correct and independently review cycle-15's **historical-source versus priced-current-visit alignment**, starting with the 11 + 8 proved cases. Preserve every forecast, the original label versions, source/target/leg identity and prior regression ledger. Re-score existing wires; no completed capture or model screen needs to restart. Do not solve uncertainty by automatic zero-row exclusions.

Then continue the same frozen current/canonical arms from their arm-matched poll-6000 native checkpoints into a new artifact directory, preferably 6000:9000 under the shared heavy lock. Keep original fleet ordering and global cache state, both physical occurrences, explicit unknowns, completed raw transitions, route/date/stopped/passed tails, and connected rider checks. No new arm search, fresh-holdout claim or application proposal is justified yet.

Owned sessions 31110, 79105, 28414, 57894, 12599, 51276, 77920 and 32034 completed; no owned server/browser/process/lock remains. Watcher, historical inputs, original builder outputs, tracked files, branch/index and controller/other-team files were not changed. UX progress/requests through its 06:29 saved-places investigation were read; no shared-source/interface action is needed. The controller retains publication ownership.
