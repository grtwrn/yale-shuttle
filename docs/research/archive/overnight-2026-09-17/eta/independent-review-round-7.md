# Independent review — ordered pickup join, slice 7

**Verdict: changes_requested. One blocking correctness finding.** The journey restoration is supported, but the raw-at-stop branch must preserve walking connection caution independently of the forecast row used for the destination join.

Exact independently verified HEAD: `1b66a1914a92a79c0936cf65473617cf7b7695f9`.
Exact supplied base and merge-base: `2dbc060bf517225c219fba8cbc5cb343fb19312b`.
Checkout and index were clean on entry and remain unchanged. No fixes, commits, branches, GitHub actions or publication occurred.

## Blocking finding

**P2 — Keep the raw-at-stop connection warning when joining through an approaching forecast.** Location: `services/shuttle-v2/web/src/TransitMap.tsx:2251` (the newly changed call and its returned journey).

This branch has established that raw GPS reports the selected shuttle at pickup, and continues to publish zero pickup ETA. Passing `forecastBoard` to `journeyArrival` also changes the evidence used for `catchRisk`: the existing helper compares walking time with `forecastBoard.low`. When that positive lower bound is at least the remaining walk, the newly restored journey is marked as a confident connection even though the rider is still walking to a bus reported already at the stop. An approach prediction is not evidence that the driver will wait.

Independent actual-built-SPA reproduction uses recorded frame1789561481036, Red#309 at Union121, a fixed plan to stop48, and synthetic live GPS110m away (100sec walk). Only the first existing pickup forecast is changed to a valid ordered row: ETA200sec, low150sec, high300sec, h1, with50 ordered quantiles. The next pickup h30, every destination forecast, raw GPS/bus identity and dwell gate are preserved. This falls inside the unchanged120sec raw boarding gate. The resulting option has pickup ETA0, walk100sec, and `journeyArrival.catchRisk === false`.

The class panel consequently displays **“Window fits buffer”** with no **“Connection uncertain”** warning or its supporting caution. Its heading remains the existing conditional “Red may fit your buffer”; I am not claiming the UI promises guaranteed arrival. A separate exact-base/current-head numerical-shell comparison confirms old status unknown versus new status fits, undefined caution and a shuttle recommendation. The new data availability must not silently remove the explicit connection-risk treatment required for someone still walking to an already-present bus.

The boundary is synthetic, not a discovered historical rider incident. A separate scan of all1172 saved Red fleet frames finds239 raw-at-stop/current-approach polls, maximum pickup low13sec, so this selected archive does not cover the positive-lower-bound/nonzero-walk boundary. No production incidence is claimed. The selector accepts longer positive pickup bounds without any causal guarantee that they describe remaining boarding time; correctness should not depend on the small lower bounds in this selected sample.

**Required correction:** in the raw-at-stop integration, retain `catchRisk` whenever `effectiveWalkToSec > 0`, while using the existing ordered forecast solely to obtain the same-bus destination window. Do not invent or mutate a zero pickup row, alter served distributions, cap ETAs, weaken ordinary approaching-bus behavior, or change bus selection. Add a regression covering a nonzero walk below/equal to a positive pickup lower bound, alongside zero walk, beyond-dwell-gate fallthrough, both occurrences and class caution/recommendation behavior. The existing new test accepting walk5sec against low5sec exercises the generic forecast helper; it does not test this raw-at-stop shell contract.

Reproducer and output: `review-round-7/walk-risk-boundary.mjs`, `walk-risk-boundary.json`, `extra.log`; exact-base isolation: `risk-logic.mts`, `shell-base.generated.mts`, `risk-logic.json`. The browser closes its context/browser in finally even on the expected failing assertion. There are no page errors.

## What independently passes

The seven-file exact supplied diff contains the previous three-file smoothing repair plus this four-file join proposal. I required the smoothing diff bytes to equal its prior approved commit's diff, and verified the three non-shell join files against controller capturefd00a7c. The merged TransitMap equals the exact supplied base with only the declared import and join replacement; its numerical block matches the frozen builder extraction. UX fullscreen merging introduced no additional numerical edit. No new model coefficient, runtime server import, wire/checkpoint schema, horizon or route-position change exists.

The selector correctly isolates route/normalized bus identity, prioritizes current zero pickup, refuses a next-lap pickup after the first destination, and leaves repeated-pickup rejection to the existing journey guard. It copies/sorts a filtered array without mutating input rows. Destination quantiles are translated by final walking time once. Existing raw pickup/countdown/distribution and both wire occurrences remain separate. Missing, malformed and stale destination data retain unavailable states in the exercised cases.

Current-source replay exactly reproduces all4688 previously reviewed ordered-prototype options, trace rows and ranking states:30 journeys restored, zero lost, zero bus/rank changes, all2344 historical150m-access decisions unchanged. The remaining38 missing cases and742 countdown/catchable identity differences remain separate. These counts describe the selected cohort, not universal ranking invariance.

The read-only connected-outcome audit independently executes13598 leg uses for all68 missing cases and1400 matched outcomes. Same bus/route, exact arrival/departure links, no skipped earlier destination and final walk accounting all pass. Changed30 outcomes span eight sources/two already-used dates: MAE880.88→396.87sec. Source63523→63632 retains the442.83sec absolute-error worsening (baseline−339.06sec; restored+781.89sec, prediction minus actual: the restored forecast is later than the observed arrival). Source61907 remains. Neither is excluded. Prior reports retain six improved/two worsened source means, seven lower/six upper misses and broad windows. This is descriptive selected-history availability evidence, not fresh-holdout accuracy, calibrated coverage, normality or observed walking success. Earlier neighbor/hazard findings remain score-dependent and do not authorize coefficients.

The carried smoothing change is unchanged from the prior approval; this finding concerns the new join. Its positive-hop/traversal guard and ordinary progress, both-occurrence, legacy/warm checkpoint, transport and shared Docker import paths pass the targeted checks. No cold-process movement-cache determinism repair is claimed.

## Independently executed commands

Application commands use this worktree's `services/shuttle-v2`. Every build/browser command holds the shared heavy.lock; all reports write this reviewer directory. Frozen builder evidence is preserved.

1. `flock -w 900 /home/gwarren/projects/yale-shuttle-watcher/overnight-2026-09-17/heavy.lock bash /home/gwarren/projects/yale-shuttle-watcher/overnight-2026-09-17/eta/review-round-7/verify.sh` — **exit0**, `verify.log`:147 tests/10 files, backend/frontend typechecks, Vite125modules/4.83sec,4688 exact prototype decisions,223 actual built-SPA numerical comparisons, keyboard/touch/44px close/focus checks, unknown/fits/buffer/late and stale/missing/recovery states, five fixed-plan live-GPS walk/dwell cases. Zero browser page errors. The command initially lists nonexistent dockerRuntime.test.ts among valid paths; it is not counted as executed. The actual Docker closure file is explicitly executed below.
2. `flock -w 900 /home/gwarren/projects/yale-shuttle-watcher/overnight-2026-09-17/heavy.lock bash /home/gwarren/projects/yale-shuttle-watcher/overnight-2026-09-17/eta/review-round-7/extra.sh` — **exit1**, `extra.log`: actual `src/server/serverEta.closure.test.ts` passes20 tests; reviewer browser then fails the material connection-caution assertion (`false !== true`). This is an application finding, not a selector/timeout/setup failure. Total passing application tests167.
3. `flock -w 900 /home/gwarren/projects/yale-shuttle-watcher/overnight-2026-09-17/heavy.lock ./node_modules/.bin/tsx /home/gwarren/projects/yale-shuttle-watcher/overnight-2026-09-17/eta/review-round-7/risk-logic.mts` — **exit0**, `risk-logic.json`: independently confirms exact-base unknown→exact-head un-cautioned fits and shuttle recommendation with the same fixture. Diagnostic exit0 confirms the defect; it is not a passing desired-behavior test.
4. `python /home/gwarren/projects/yale-shuttle-watcher/overnight-2026-09-17/eta/review-round-7/verify_evidence.py` — **exit0**, `evidence.log/.json`: exact source/diff provenance, all4688 reproduced candidate states,13598 connected-leg uses/1400 matched outcomes, retained worst regression and all101 frozen cycle6/cycle7 file hashes unchanged. It reuses the previous review's inspected chain-verification logic with current candidate output and exact supplied SHAs; no historical records are changed.
5. `git diff --check 2dbc060bf517225c219fba8cbc5cb343fb19312b HEAD && git diff --exit-code && git diff --cached --exit-code && git status --porcelain && git rev-parse HEAD && git merge-base 2dbc060bf517225c219fba8cbc5cb343fb19312b HEAD` — **exit0**, exact identities and clean checkout/index. Final integrity is saved separately.

No full suite, backend staging, Docker image build, physical phone, native screen reader or production validation was performed in this review. Controller retains those publication gates after correction/review. Browser fixtures intercept all requests and use `seedTestId`; nothing contacts production analytics or feedback. No screenshot or dependency installation was added. Earlier research/reviewer failures remain preserved; one read-only sed inspection typo was rerun correctly and did not affect any check.

## Resume and coordination

Fix only the raw-at-stop catch-risk integration, rerun the failing browser/logic boundary with desired caution assertions and the existing successful suite, and submit a fresh independent review. Keep smoothing publication independently identifiable. Preserve the30 restored journeys and documented regressions; do not broaden to the38 outgoing cases or alter catchable/countdown identity/ranking.

Latest UX02:23 progress is a separate identity-presentation candidate and does not change this numerical block. ETA REQUESTS records the required caution contract. All owned command sessions completed, contexts/browsers closed, locks released. No owned server or daemon remains; the existing watcher is untouched. Tracked source/index/branch, other-team files, historical DBs, credentials and controller files/state are unchanged.
