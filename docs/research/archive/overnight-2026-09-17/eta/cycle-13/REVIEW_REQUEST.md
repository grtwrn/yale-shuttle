# Independent research review requested

No app diff or release candidate. Start with RESULTS.md, RECEIPT_RESULTS.md,
cache/RESULTS.md and both frozen plans. Preserve original outputs; copy scripts
and reroute their absolute output directories before reproducing cache work.
Receipt scripts derive output directory from __file__; cache scripts currently
hardcode their builder output directory. Do not overwrite it. App imports/cwd
remain the ETA checkout, and source/input hashes pin8aa67bd.

1. Verify all487sample extractions against original file/line and complete
   ambiguity-preserving candidate sets. Confirm130exact surrounding matches
   are not misrepresented as exact captures of any of38decision timestamps.
   Eight unique-compatible,14ambiguous and16unmatched decisions stay visible.
2. Confirm watcher handler clocks versus collection/publication clocks, the
   absence of server_eta and four source-window coverage gaps. Do not certify
   original live server belief or doors-open boarding from these samples.
3. Verify moveKernel's first-caller dependence directly from current source.
   The diagnostic replacement must be only canonicalizing the mean to the
   existing cache-key precision; compare fresh separate processes.
4. Confirm2,178prefixpolls/78windowpolls per arm, both three-bus checkpoints,
   78same-process matches, allfouridentical input windows and exact canonical
   full-state/full-wire fresh-process parity. Currentcode is the comparator,
   not cycle4's old forecast outputs. Historical calibration is frozen.
5. Independently audit quantile joins and both occurrences. Preserve8existing
   negative lower bounds; no cap/exclusion. Inspect largest tail case and
   posterior gate0.807607→0.786230across0.8. Median unchanged there is not proof
   of harmless tails. Current versus canonical warm changes remain material.
6. Verify no accuracy/all-route/release claim and no app/HEAD/index change.
   Retain1,400old outcomes,38unknowns and exact442.82701916224846sec regression.
   Retained harness failures and score corrections must remain visible.

Next: research verdict, then controller-scoped complete/all-route causal paired
replay with connected outcomes and restart/order checks for the fixed diagnostic.
Do not publish an unvalidated tracker change or restart completed model screens.
Cycle12's interrupted review has no final acceptance; it may be completed on
its own evidence without repeating this new experiment. UX consumer review is
separate. No active owned process, browser, server or shared lock remains.
