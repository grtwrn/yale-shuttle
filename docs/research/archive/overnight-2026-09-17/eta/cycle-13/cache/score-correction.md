The first supplemental quantile maximum assumed pairs [rowIndex,vector]; actual wire distributions are directly aligned 50-value vectors. First score retained. Corrected maxima and added schema assertions plus separate ETA/bound/departNow/lowFloor deltas. Full-wire and belief equality checks were already correct, and replay data did not change.
Independent verification then caught row-order differences: wire rows can reorder with ETA changes. Final quantile audit joins by bus/stop/occurrence rather than zipping row indices, checks all paired rows, and retains earlier score JSON. Two verifier attempts are retained: first discovered eight negative lower bounds already identical in every arm; second assumed identical wire ordering. These are explicitly accounted for; no data or estimator changed.
