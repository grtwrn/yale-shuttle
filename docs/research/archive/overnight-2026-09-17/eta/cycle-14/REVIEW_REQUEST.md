# Research review requested — cycle14

No application diff or release candidate. Read RESULTS.md and the three frozen
plans. HEAD98e535b99649e74ca599d2e33bcfdc46df83d30d remains unchanged.

1. Verify the exact single canonical-tenth replacement and separate process
   arms. Both release ON, same chronological calibration and original input
   order. Confirm151full-server checks per arm and150020exact old release-ON
   baseline rows after existing adapter normalization. Red-only process state
   is not full-fleet cache state; that limitation is explicit.
2. Audit physical occurrence joins,202hop differences, unchanged first/second
   classification and availability. Confirm327connected journey uses,
   6674leg uses,3309checkpoint truths,801second pairs/318absent. Preserve all
   legitimate old outcomes and unrepresented older source IDs without claiming
   they were newly validated. The raw h==29 boundary remains explicit.
3. Independently reproduce aggregate metrics, date/stop/checkpoint and
   passed/stopped summaries. Supplemental83757poll-target rows have unequal
   repeated-poll weighting; do not treat them as independent sample sizes.
4. Inspect allfive >60sec band changes and four post-arrival-label cases
   excluded by the old scorer's pre-arrival window. Values/nearby records stay
   visible, no new truth label or cap. Preserve137/136negative low bounds,
   39.299sec largest all-poll regression, new/resolved tail misses and extra
   downward jumps. No inferred accuracy improvement or harmless-tail claim.
5. Verify eight-route fixture, both repeated-stop routes, three truly fresh
   restart processes per arm and complete map/typed-array state preservation.
   Compare physical keys/quantiles;123current replaced keys are not123lost
   total arrivals. Reversal passes both arms; canonical restart60/60passes,
   current0/60. This is not all-route outcome validation.
6. Verify197354exact fresh rows from20199all-route polls, readonlyDB/hash and
   causal collector reconstruction limits. Review saved topology repair and
   retain repeated occurrences. Reuse exports, do not restart extraction.
7. Confirm clean HEAD/index/app, no screenshot/browser/process/lock/watcher
   mutation and no CI/build/type/deploy claims. Prior cycle12 research review
   remains separately unfinalized; this request does not grant its approval.

All scripts derive own output path except export-all-route.mts, which has the
explicit cycle14 output directory: change it in a reviewer copy before running.
Read-only input paths stay fixed. run.sh/run-fixture.sh require shared heavy.lock
and use this checkout's dependencies. Artifact scripts must never overwrite
old experiment/controller outputs.

Next task: fixed-diagnostic bounded full-fleet connected-outcome validation on
the already-exported12routes, including repaired/repeated-stop matching and
rider choices. No new model/threshold search or direct one-line release.
