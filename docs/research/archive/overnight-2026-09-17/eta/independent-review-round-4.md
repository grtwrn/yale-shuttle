# Independent review — round 4

**Verdict: research_only.** Exact supplied base and independently read HEAD are both `cff3b2a6b7ad6cdd2f899bb4d564a4c03dae2d37`. There is no candidate application diff and no production improvement to approve. The pooling defect is independently reproduced and merits the next builder slice. I also localized the saved fixture's fresh-process divergence to a movement-kernel cache. No tracked file, branch, index, historical database, controller state or publication was changed.

## What is established

- Reviewer-authored `review-round-4/verify_evidence.py` passed all15 frozen input hashes and61 unchanged builder file hashes. All1,172 captured fleet inputs agree with the archived raw observations and the frozen window membership. The160,496 row/distribution identities are unique, finite and ordered. Independently matched6,025 current wire rows and complete50-point distributions to cycle2.
- The original full capture's11,081-poll execution was inspected and its saved output verified; I did not repeat that entire experiment. It steps all observed Red buses continuously, resetting across recording gaps, with current release ON and historical calibration predating evaluation. It does not read future outcome labels into forecasts. The independent2,178-poll prefix below additionally reproduces one complete selected window from actual code.
- Independently counted1,734 zero-hop rows:101 have a positive ETA,90 at Winchester(stop11) and11 at ordinary stop146. Of these,84 put the pickup later than a downstream arrival. All six observed h29→h0 transitions agree with the30-second pooling equation within one second of saved transport rounding.
- `eta/arrival.ts` emits the already-arrived row with zero ETA/bounds/distribution; `eta/index.ts:201` identifies pooling memory by stop ID and ordinal occurrence. Its `row.stopsAhead <= old.row.stopsAhead` guard admits h29→h0. The ordinal has changed physical meaning. This is an estimator invariant violation, independent of whether a forecast median was accurate on a particular journey.
- Reexecuted the exact-source raw-row observer under the lock:423 same-process wire comparisons,33 raw zero rows,19 overwritten with positive pooled values. The generated observer was independently reconstructed from current source and is identical apart from the declared import resolution and raw-row clone. Both occurrences remain recorded.
- Reexecuted the40-session decision path:4,688 rows,4,471 destination checks. Output decisions and metadata are byte-identical to the builder artifacts. Verified the generated live-map body is exactly the current shell body, including its SHA256. Stable boarding/alighting/planned-ride choices remain fixed; all48 order changes meet30-second persistence. No ranking-threshold defect is established.
- Reexecuted both complete58224→48 browser sessions using the existing built SPA, intercepted local fixtures and tester identity:156 numerical option/order comparisons, no page errors. Both contexts and the browser closed; no screenshots or server were created. This checks actual-shell React numerical state for two sessions, not the rendered text at every poll, every view, all40 sessions, a screen reader or a physical phone.
- Independently revalidated all1,420 current-source outcome labels using12,964 exact connected leg-record uses and20 source/target contexts. There are1,382 modeled reachable connections and38 modeled access misses; all remain. Of the matched rows,175 lack a full destination distribution. Recomputed early/late flags and physical predicted-minus-actual error signs.
- `verify_accounting.py` independently assigns every decision to the scored subset or the retained missing category:1,892 different boarding stops,1,104 different buses,144 outside the source visit and128 noncurrent boarding occurrences. No alternative receives the focal bus's truth. It reproduces676 countdown/journey bus differences and the217 missing-journey split:180 no modeled zero-time board,37 destination-before-pickup/access. Those217 are not one proven defect.

The report's481 reachable matched rows preferring Red despite a slower eventual bus arrival than modeled walking, and zero opposite-direction cases, reproduce. They remain selected conditional diagnostics, not population regret or observed rider behavior. The origins and access walks are hypothetical;150m west does not establish street access. The data contain only Red, previously inspected dates and reconstructed collector metadata. Repeated polls/shared targets are dependent. No fresh holdout, calibrated probability/coverage, new exclusions or coefficients follow. Earlier neighbor/service-role evidence remains mixed by score, weighting and date; this round does not change those conclusions.

## Fresh-process divergence is localized for this fixture

The builder correctly reported nonparity. Reexecuting the observer-free diagnostic produces1,828 changed numeric rows with maximum3,768sec across78 frames. Its exit0 is a diagnostic success, **not** forecast parity. The wire's coarse tracking fields and occurrence availability match; this does not prove the full posterior arrays match.

New independent evidence identifies `eta/filter.ts:388`–`moveKernel`: the cache key rounds mean cells to tenths, but kernel length/probabilities use the unrounded first mean entering that bucket. Consequently `moveKernel(1.01)` can return a different kernel depending on whether1.04 was requested first. The saved per-bus checkpoint omits this process-global state.

I generated an artifact-only import closure of six current modules, resolving imports and exporting/recording cache misses without changing arithmetic. Its continuous2,178-poll prefix reproduces **all78 complete expected wire payloads exactly**, including every distribution, and records21 first-use kernel means **before** the checkpoint window. A fresh unprimed process reproduces the original1,828-row/3,768sec mismatch. Priming only those21 kernel means makes all78 complete wires exact.

Finally `verify_native_primed.mts` imports only the unmodified application modules, primes the original `moveKernel` using those prefix-only means, restores the original checkpoint and reproduces **78/78 wires,10,592 rows and their full distributions**, with three restored beliefs and no server failures. This isolates the cause for the selected fixture without relying on the generated observer's forecast implementation.

One reviewer harness failure is preserved: the first cold comparison aligned row arrays by position, but forecast sorting can change row order when times differ. It failed `2 !== 0` on bus index. The corrected comparison uses exact(bus,stop,hops) identity; no app output, tolerance or expected values changed. The78-frame **primed** checks still require exact full-wire equality, including ordering.

This finding does not certify all checkpoints or justify changing the tracker within a pooling fix. The21 recorded means are fixture-specific diagnostic state, not production coefficients or a repair. Continue full-prefix baseline/candidate comparisons, or use this proven fixture priming only where explicitly scoped. A separate deterministic-cache/restart change needs its own controller-approved tracking scope, all-route/forward-progress evidence and fresh independent review.

## Next coherent builder slice

Repair pooling occurrence identity in ETA-owned code. Preserve the raw already-arrived row and prevent memory from migrating between physical laps for both first and following occurrences. Do not merely clamp positive zero-hop rows while leaving other occurrence aliases unexamined. When identity cannot be established, bypass stale pooling rather than invent a capped time. Preserve legitimate ETA increases and route-forward belief behavior.

Use the saved continuous-prefix inputs and current release-ON historical calibration for a paired comparator. Retain all existing valid regressions, availability/horizon omissions, short/long journeys and both occurrences. Check actual pickup/destination consistency, forecast distributions, departure response and jumps, not just a scalar zero invariant. Include ordinary stop146 and the58224 h29↔h0 sequences. Require substantive regression tests, backend/frontend types, runtime-import closure and applicable full build/staging gates before code approval. The movement-kernel cache cause is now documented; do not silently mix a tracking change into this repair.

Shared planner/raw-at-stop fallback and UI treatment remain separate coordinated follow-ups. UX's current history presentation proposal changes no estimator/wire semantics; its PROGRESS/REQUESTS through00:29 were read and there is no source conflict. No new cross-team edit is requested.

## Executed commands and outputs

All artifact paths below expand under `/home/gwarren/projects/yale-shuttle-watcher/overnight-2026-09-17/eta`; TS/browser commands ran from the ETA checkout's `services/shuttle-v2`. `O` below denotes that absolute `review-round-4` directory, and `L` denotes `/home/gwarren/projects/yale-shuttle-watcher/overnight-2026-09-17/heavy.lock`.

- `python $O/verify_evidence.py` — exit0; independent input/wire/pooling/chain/ranking/source checks, `evidence.log/.json`.
- `flock -w 900 $L bash -c 'set -e; ./node_modules/.bin/tsx $O/observe_pooling.mts; ./node_modules/.bin/tsx $O/verify_fresh_fixture.mts; ./node_modules/.bin/tsx $O/decision_replay.mts; node $O/browser_parity.mjs'` — exit0, `reproduction.log`. Actual execution used the full absolute paths, not unexported O/L variables. The four commands produced reviewer-only outputs and left builder evidence intact.
- Inline Python byte/provenance comparison — exit0: decisions, metadata, observer and fresh-fixture JSON byte-identical; exact shell body/hash match.
- `python $O/prepare_kernel_probe.py` — exit0; reviewer-only closure/source hashes.
- `flock -w 900 $L bash -c 'set -e; ./node_modules/.bin/tsx $O/kernel_probe.mts prefix; ./node_modules/.bin/tsx $O/kernel_probe.mts cold; ./node_modules/.bin/tsx $O/kernel_probe.mts primed'` — exit1 due solely to the first cold harness's row-order assumption; prefix passed2,178 frames/78 exact complete wires. Preserved `kernel-probe-first-row-order.log` and original `kernel-probe.log`.
- `flock -w 900 $L bash -c 'set -e; ./node_modules/.bin/tsx $O/kernel_probe.mts cold; ./node_modules/.bin/tsx $O/kernel_probe.mts primed'` — exit0 after identity-based comparison, `kernel-restart.log`: cold mismatch reproduced; primed78/78 exact.
- `flock -w 900 $L ./node_modules/.bin/tsx $O/verify_native_primed.mts` — exit0, `native-primed.log/.json`: unmodified modules exact78/78 complete wires/10,592 rows and distributions.
- `python $O/verify_accounting.py` — exit0; complete unavailable/unmatched/countdown identity accounting, `accounting.json`.
- `git diff --check && git diff --exit-code cff3b2a6b7ad6cdd2f899bb4d564a4c03dae2d37 HEAD && git diff --exit-code && git diff --cached --exit-code && git status --porcelain && git rev-parse HEAD && git rev-parse cff3b2a6b7ad6cdd2f899bb4d564a4c03dae2d37` — exit0; all diffs/status empty and exact SHAs verified.

No independent dependency install, model fit, typecheck, full Vitest, Vite rebuild, Docker or staging was needed or claimed for this research-only review. Browser used the builder's existing build. No daemon/watcher, historical archive, private feedback, analytics, credentials, screenshots, controller files or GitHub were touched. All command sessions completed and shared locks released; final integrity checks are recorded in PROGRESS.
