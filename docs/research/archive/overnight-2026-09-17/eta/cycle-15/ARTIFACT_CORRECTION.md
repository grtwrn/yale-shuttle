The cycle-14 original fixture's per-poll ModelEntry proof is superseded by independent reviewer13's immutable fixture and audit. Its shallow entries referred to objects later mutated during replay. All original cycle14 files are preserved. Original wires remain supported; original per-poll state equality is not reused.

This cycle serializes each wire/tracking record immediately, saves full poll-time state via structuredClone before hashing, and asserts that no saved belief timestamp lies after that poll. Checkpoints use immediate native v8 serialization of full server state and the first-caller kernel cache. This extra cache capture is diagnostic harness code only. Its validation is the fresh-process continuation comparison, not an assumption that production checkpoint includes cache state. The earlier independent diagnostic's production-checkpoint restart proof remains in review-round-13/immutable; it need not be repeated.
