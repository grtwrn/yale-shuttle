# Independent review — walking caution and PR288 integration

**Verdict: approve. No blocking findings.** The round7 walking-caution defect is corrected in the actual candidate. The ordered destination join and previously approved smoothing repair remain intact alongside production's distinct-bus trip presentation.

Exact independently verified HEAD: `53b4076b6273deb6f4e2ad436420461a0e5174f4`.
Exact supplied base and merge-base: `373505d5076f21a08de69d9587be9b2a55956642`.
The checkout and index were clean on entry and remain unchanged. No tracked file, branch, commit, controller state, GitHub or deployment was modified. This review approves the candidate for the controller's remaining publication gates; it does not claim deployment.

## Correction and source provenance

The complete seven-file diff exactly equals the builder's frozen `cycle-9/proposal.patch`. Each changed file equals its committed HEAD blob and frozen source hash. The entire merged TransitMap equals the exact production base with only the ETA import and the declared numerical block replaced. The block matches the captured correction commit0d0f34fa. PR288's `tripBusIdentity` and `TripBoardingActions` imports and presentation are retained. The three-file smoothing delta exactly equals the round5-approved diff; no hidden conflict-resolution arithmetic change exists.

`journeyArrival` now accepts explicit pickup context, defaulting to existing forecast behavior. Only the raw-at-stop branch passes `at-stop`. In that context any positive remaining walk sets `catchRisk`, regardless of the approaching pickup lower bound used to join the destination. A rider already at pickup retains the zero-walk behavior. The ordinary approaching branch continues to compare walk time with the pickup lower bound. The destination point/bounds/distribution, final walk, raw pickup values, dwell gate, bus selection and ranking logic are unchanged by the caution correction.

The selector uses only already available same-route/same-bus forecast rows. It prefers an existing current pickup, otherwise requires pickup before the first destination, with the existing repeated-pickup guard preventing the wrong folded-route visit. It never invents a zero forecast or borrows a later destination lap. Missing or invalid destinations remain unavailable. Transport normalizes identities before this join, and sorting a filtered array does not mutate the wire rows.

The carried smoothing repair keeps zero-hop rows exact and avoids pooling between different future traversals. It retains ordinary forward progress, uncapped legitimate increases and both occurrences. There is no new coefficient, calibration input, route-position rule, runtime server import, wire schema or checkpoint schema. The unchanged process-global movement-kernel cache limitation remains separately documented; this proposal does not claim to fix cold-process determinism.

## Independently executed verification

Application commands ran in this checkout's `services/shuttle-v2`; every build/browser/accuracy command acquired the shared heavy.lock. Reviewer copies write only `review-round-8`. Builder outputs were inspected and then independently reproduced, not accepted solely from their logs.

1. `flock -w 900 /home/gwarren/projects/yale-shuttle-watcher/overnight-2026-09-17/heavy.lock bash /home/gwarren/projects/yale-shuttle-watcher/overnight-2026-09-17/eta/review-round-8/verify.sh` — **exit0**, `verify.log`:221tests/15files, backend and frontend typechecks, Vite127modules/4.79sec,4688exact historical decision/trace/ranking states,223actual built-SPA numerical comparisons, five walking/dwell-gate cases, the original review7 reproducer, six rendered caution boundaries and production's mobile/desktop trip-identity checks. Includes server parity, warm checkpoint/restoration, corrupt/old checkpoint behavior, both occurrences, freshness and20Docker import-closure tests.
2. `flock -w 900 /home/gwarren/projects/yale-shuttle-watcher/overnight-2026-09-17/heavy.lock npm run test:accuracy` — **exit0**, `accuracy.log`:175tests/nine files. Adds the existing layover, approach-rest, arrivals and anchor regression gates. There are285distinct application tests across these two commands;396executions include111overlapping tests.
3. `./node_modules/.bin/tsx /home/gwarren/projects/yale-shuttle-watcher/overnight-2026-09-17/eta/review-round-8/risk-logic.mts` — **exit0**, `risk-logic.log/.json`:eight identical-input pairs against the explicitly frozen pre-correction1b66 shell/helper. Only the raw-at-stop catchRisk/class caution changes; destination/pickup values, route/bus and ranking remain identical. This old diagnostic comparator isolates the previous defect; it is not mislabeled as today's production baseline.
4. `python3 /home/gwarren/projects/yale-shuttle-watcher/overnight-2026-09-17/eta/review-round-8/verify_evidence.py` — **exit0**, `evidence.log/.json`: exact head/base/diff/source provenance,4688current-source decisions,68missing-case identities,13598read-only connected-leg uses/1400matched outcomes, retained worst regression and183unchanged builder artifacts. The reviewer verifier explicitly replaces the builder's pending-merge assertions with the supplied committed head/base; original builder verifier and plans remain untouched.
5. `flock -w 900 /home/gwarren/projects/yale-shuttle-watcher/overnight-2026-09-17/heavy.lock node /home/gwarren/projects/yale-shuttle-watcher/overnight-2026-09-17/eta/review-round-8/state-transitions.mjs` — final **exit0**, `state-transitions.log/.json`: reviewer-authored single-page sequence checks11states and reload. The same100-second walk/150-second pickup lower-bound fixture changes raw-at-stop → approaching → raw-at-stop; live GPS reaches pickup and walks away; destination disappears/recovers; forecast rows become empty/recover; feed returns503/recovers. Caution appears only for the positive-walk raw-at-stop connection, destination point remains constant across available states, missing states show “No live window”, and reload retains the caution. All browser errors are empty and resources close in finally.
6. `git diff --check 373505d5076f21a08de69d9587be9b2a55956642 HEAD && git diff --exit-code && git diff --cached --exit-code && git status --porcelain && git rev-parse HEAD && git merge-base 373505d5076f21a08de69d9587be9b2a55956642 HEAD` — **exit0**, exact identities and clean checkout/index. Final source/artifact integrity is also saved in `review-round-8/final-integrity.json`.

The initial two lifecycle invocations exited1 on reviewer harness assumptions, not application assertions: first, the rendered warning includes a “View trip” suffix and is not an exact standalone text node; second, unavailable options return the saved plan with etaUnavailable and need not have live `departed`/walk fields. The matcher now tests the actual unavailable contract and continues to require no journey plus the rendered unavailable state. Both failed scripts/logs/JSON remain as `.first`/`.second`; no application code or expected warning behavior was changed. A read-only inline screenshot-census comprehension also had a variable-shadowing error, then passed when corrected; it did not affect application checks.

## Rider behavior and statistical limits

The original positive-lower-bound defect now passes in the built app: a100-second walk with pickup low150 keeps “Connection uncertain” and “This window assumes you catch it.” A class window fitting the buffer is not offered as an unqualified shuttle recommendation. Zero walk, exact walk/lower-bound equality,119sec inside the dwell gate,121sec outside it and ordinary approach are separately exercised. Class unavailable/fits/tight-buffer/late states remain distinct.

Keyboard Enter/Escape, touch,44px close targets, restored focus and phone reflow pass. PR288's two named boarding actions, countdown#307 versus journey#309 attribution, final walking directions and both pickup slots pass on mobile and desktop. Three reviewer screenshots were visually inspected, including the rendered raw-at-stop caution. These local fixtures intercept all network requests, use seedTestId, and do not contact production analytics. They are not physical-phone or native screen-reader tests.

All4688current-source ordered-join decisions equal the previously reviewed prototype. The join restores30journeys with none lost, and its2344selected nonzero-access decisions and selected bus/ranking states are unchanged. **That comparison is incremental to the already-reviewed smoothing candidate**, not a claim that the whole seven-file proposal changes only30decisions against production. The earlier smoothing repair separately restored149journeys and carries its accepted availability/invariant tradeoff. Its broader replay was already independently executed in round5 and was not restarted here. Current base's intervening changes are presentation-only; its estimator sources remain the prior replay comparator.

The read-only chain audit revalidates1400connected outcomes and all68missing cases. The30join changes span eight source visits/two already-used dates; their descriptive MAE880.88→396.87sec is retained, not a new gain from this caution correction. Source63523→63632 still worsens absolute error442.83sec: baseline−339.06sec, restored+781.89sec, prediction minus actual. Source61907 also remains; neither is excluded. Earlier smoothing's modest paired MAE316.20→319.06sec and228.51sec largest added-error case remain in round5's accepted review. Those are separate cohorts/comparisons and must not be added or relabeled as one production accuracy estimate.

These selected repeated historical checkpoints are dependent, use reconstructed collector context and hypothetical walks, and are not fresh holdout or observed rider catch outcomes. Seven lower/six upper misses among the restored join windows remain reported. No nominal coverage, calibrated on-time probability, normality or universal ranking invariance follows. The earlier neighbor/service-role screens remain score-/weighting-dependent and exploratory, and authorize no new coefficients. No future neighbor trajectory, outcome-based exclusion or arbitrary ETA cap enters this candidate.

## Controller handoff

**Approve this exact candidate.** Controller retains full-suite, staging API/browser smoke, CI, serialized integration/merge and deployment verification. This reviewer did not run full-suite/backend staging/Docker image build or make production requests; targeted Docker closure and actual built-browser coverage are documented above.

After publication gates, continue a separately scoped ETA/UX contract for the chosen boardable pickup's bus and occurrence independent of destination availability. The38outgoing unknown cases and same-bus later-visit ambiguity remain unresolved; do not infer occurrence from matching vehicle numbers or force a raw board-now forecast. Keep movement-cache determinism separate and do not restart completed negative model screens.

All owned sessions are complete, browser pages/contexts/browsers closed, and heavy locks released. No owned server or persistent watcher was launched. The existing watcher is untouched. No dependency installation, historical-record mutation, other-team artifact edit, credential access or controller-file edit occurred. Combined screenshot census at capture:171files/7,725,432bytes, below100MiB; earlier evidence is preserved.
