# Research review requested: selected downstream trace

No app proposal, coefficient or production change is ready. Please review `TRACE_REPORT.md` and the scripts/data it links. Exact HEAD remains40af3c0; all work is in ETA artifacts. Do not rerun cycle1 or fit another model for this review.

Review priorities:

1. Current release-ON comparator, archived causal calibration and continuous warm states;242 exact archived checkpoint matches and163 full-server checks.
2. Outcome chain identities and occurrences:20 first endpoints, eight complete full-lap second endpoints,12 explicitly incomplete second chains, five Winchester diagnostic endpoints. Preserve arrived_at versus pin clock and shared target clustering.
3. Artifact-only sample observer: generated `.mts` imports the same live module state as current code.25 exact row/quantile parity checks and5,120 additive sample identities; no realized future event enters forecasting. Means before display corrections must not be called served medians. The superseded `.ts` harness failed module-state parity and is marked; do not use it.
4. Missing second forecasts and existing horizon:14 unavailable checkpoint rows remain visible; the five source+60 second-Rosenkranz omissions are verified against the existing raw lead-chain5400-second gate. No new cap is proposed.
5. Ten new intermediate GPS audits establish coordinate coverage and retain legitimate delays, without equating unchanged coordinates to exact stillness or claiming actual pin publication times.

Useful inexpensive checks: run `score_trace.py` against preserved trace/labels, verify hashes against `TRACE_PLAN.json` and `trace-artifact-hashes.json`, and under the shared heavy lock run local `tsx probe_components.mts` from v2 against the five saved `component-snapshots.v8` states. The full trace need only rerun if a concrete discrepancy requires it. Final logs and limitations are in `TRACE_REPORT.md`; all spawned command sessions have completed. No browser, watcher or server was launched.

Disposition requested: independently assess the research-only conclusion, then advance backlog4’s service-role state question. The prior two-history slot/survival result already exists and is not a new experiment to repeat. No source/clock repair follows from these selected cases alone.
