# Next bounded task: cancel zoom teardown callbacks safely

This is now browser-reproduced on the production baseline, not just the previous fake-clock lead. Preserve UX09 crash-recovery proposal and review it independently; no map fix is included there.

Read `map-diagnostic.json`, `map-stress-current/map-stress.json`, `map-stress-baseline/map-stress.json` and `stress-compare.log`. Both current and frozen production-entry builds emit the same two `_leaflet_pos` exceptions after one normal-clock iteration. `map-diagnostic.json` proves both bundles contain byte-identical current/production `TransitMap.tsx`; the frozen entry is exact captured PR292 `main.tsx`. All other application source is unchanged from the clean base. Baseline artifact build used a Vite load hook to substitute only the saved entry, with output in `baseline-dist`; no checkout/source/HEAD change.

Sequence: Map → click Zoom in → immediately Trip → keyboard Map → Zoom out → Hide all → Show all routes → Trip. It uses the real built app and a local synthetic two-bus feed, actual browser clock and a bounded port8093 server. No live service or DB is involved. Original longer stress script and failure are preserved as `map-stress-original.mjs`, `map-stress/` and `map-stress.log`; it timed out during Hide all with 59 asynchronous exceptions. Reduced current/baseline scripts each stop after the first error-bearing iteration and exit 1 on their error assertion. The shell comparison wrapper exits 0 because it deliberately captures both expected diagnostic failures; that is NOT a passing map test.

Stack: `_onZoomTransitionEnd` → `_move` → `_getNewPixelOrigin` → `_getMapPanePos` → position lookup on removed pane. Inspect installed Leaflet `src/map/Map.js`: `_animateZoom` schedules a 250ms timeout for `_onZoomTransitionEnd`; `remove` removes/deletes `_mapPane`; the callback checks `_animatingZoom` before subsequently moving. Existing AllRoutesMap cleanup in `TransitMap.tsx` around 1544 already tries `map.stop()` before `map.remove()`. That is insufficient in this reproduction. Treat the timer explanation as a source-supported diagnosis; do not claim every other map is affected without reproducing.

Next builder should freeze the minimal normal-clock repro, implement a small supported lifecycle correction, and verify both rapid zoom/view-unmount and rapid filter rebuild on phone and desktop. Audit other map cleanups (lines approximately 650,1161,5319,6373 plus berth components) for the same mechanism before deciding whether a shared helper is warranted. Preserve map interactions, route filtering, geometry, arrival identity and focus. Do not edit dependencies or silence global errors. The recovery page does not fix these asynchronous errors: the app shell remains and the test receives pageerror, without entering the React fallback.

Commands (from services/shuttle-v2, all complete server/browser start-test-cleanup held under heavy.lock):

`OUT=<fresh evidence directory> flock -w 900 /home/gwarren/projects/yale-shuttle-watcher/overnight-2026-09-17/heavy.lock node /home/gwarren/projects/yale-shuttle-watcher/overnight-2026-09-17/ux/ux-09/map-stress.mjs`

Optional `DIST_ROOT=/home/gwarren/projects/yale-shuttle-watcher/overnight-2026-09-17/ux/ux-09/baseline-dist` targets the already-built baseline. Preserve existing evidence; no need to rerun the completed baseline build or experiments just for fresh context. Script expects no errors, so exits 1 on the current defect. Every recorded run closed its browser/context/page/server. No owned process remains; existing watcher untouched.
