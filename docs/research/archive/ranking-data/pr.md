Live ETA changes were also changing the inferred ride duration used to compare routes, so short direct routes could reshuffle or disappear. Ranking now retains planned ride duration, prefers shorter walks and rides when destination windows overlap, and requires a changed arrival advantage to persist for 30 seconds. Clearly earlier destination windows still take priority; unavailable routes move down immediately.

Routes that save almost no walking are removed in both planners and the geographic fallback. The minimum saving is 20% for short walks, two minutes for 10–20 minute walks, and 10% for longer walks. When a destination forecast is missing, the pickup lower bound prevents a clearly late bus from winning on short ride length alone. Third-route visibility uses planned travel instead of changing waits.

Validation: full 2,715-test suite before final review refinements; final 124 focused tests, backend/web typechecks and build; phone browser checks for Division/Prospect → Rosenkranz, class deadlines, history, waiting labels and failure flows; six synthetic large-ETA changes with live text and stable order. Statistical reviewer checked ordering and caught a runtime dependency omission, now fixed with Docker COPY plus expanded import-closure coverage. The built backend container successfully imports both planners and the estimator and passes the final walking-policy check. Final browser checks also pass after the review refinements.

ETA-model values and interval widths are unchanged. The urgent Red 1–19 minute model issue remains separate; the no-ratchet experiment worsened departure errors and is not included.
