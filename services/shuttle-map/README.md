# ARCHIVED v1 — not deployed

This is the v1 frontend/backend (FastAPI `server.py` + the original
`app/src/TransitMap.tsx`). It has not served production since 2026-06-04;
the live app is `services/shuttle-v2/` (`services/shuttle-v2/web/src/TransitMap.tsx`
is the file that renders https://yale-shuttle.fly.dev).

Edits here compile and lint fine but change nothing in production — this has
bitten before. Keep this tree for reference only.
